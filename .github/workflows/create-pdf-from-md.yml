name: Markdown to PDF

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'pms/**/*.md'
      - 'pms/images/**'
      - 'images/**'
      - '.github/workflows/create-pdf-from-md.yml'
  release:
    types: [published]

jobs:
  topdf-with-image:
    name: Markdown zu PDF
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Abhängigkeiten installieren
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc
          python3 -m pip install --user weasyprint

      - name: Markdown nach PDF umwandeln
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import subprocess

          repo = Path.cwd()
          source_dirs = [repo / 'pms', repo / ]

          for src_dir in source_dirs:
              if not src_dir.exists():
                  continue
              for md_file in sorted(src_dir.rglob('*.md')):
                  target = md_file.with_suffix('.pdf')
                  cmd = [
                      'pandoc',
                      str(md_file),
                      '-o',
                      str(target),
                      '--standalone',
                      '--pdf-engine=weasyprint',
                      '--resource-path',
                      f'{src_dir}:{repo}',
                  ]
                  print('Konvertiere', md_file, '->', target)
                  subprocess.run(cmd, check=True, cwd=repo)
          PY

      - name: Gefundene Dateien anzeigen
        run: |
          echo "PDF-Dateien gefunden:"
          find pms -type f -name "*.pdf" | sort

      - name: Artefakte hochladen
        uses: actions/upload-artifact@v4
        with:
          name: alle-pdfs
          path: |
            pms/**/*.pdf
          if-no-files-found: error