# .github/workflows/convert-to-pdf.yml

name: Markdown to PDF and HTML
# This workflow is triggered on pushes to the repository.
on:
  workflow_dispatch:
  push:
    branches:
      - main
    # Paths can be used to only trigger actions when you have edited certain files, such as a file within the /docs directory
    paths:
      - 'pms/**.md'
      - 'pms/images/**'
      - 'sonstige/**.md'
      - 'images/**'
  release:
    types: [published]

jobs:
  topdf-with-image:
    name: Markdown to PDF mit Bild
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: baileyjm02/markdown-to-pdf@v1
        with:
          input_path: pms
          output_dir: pdfs-pms          # ← kein Unterordner!
          images_dir: docs/images
          image_import: ./images
          build_html: false

      - uses: baileyjm02/markdown-to-pdf@v1
        with:
          input_path: sonstige
          output_dir: pdfs-sonstige     # ← kein Unterordner!
          images_dir: images
          image_import: ./images
          build_html: false

      - name: Debug – Verzeichnisstruktur anzeigen
        run: find . -name "*.pdf" -o -name "*.html" | head -50

      - uses: actions/upload-artifact@v4
        with:
          name: alle-pdfs
          path: |
            pdfs-pms
            pdfs-sonstige